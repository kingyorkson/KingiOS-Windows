<Page x:Class="KingIOS.Views.InstallOptionsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="#1A1A2E"
      Title="Install Options">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#16213E" BorderBrush="#2A2A4A" BorderThickness="0,0,0,1">
            <Grid Margin="10,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="← Back" 
                        Command="{Binding GoBackCommand}"
                        Background="Transparent" Foreground="#A0A0B0" BorderThickness="0" FontSize="14" Cursor="Hand"/>
                <TextBlock Grid.Column="1" Text="Installation Options" FontSize="20" FontWeight="Bold" Foreground="White" VerticalAlignment="Center" Margin="20,0,0,0"/>
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="1" Margin="30">
            <StackPanel MaxWidth="600" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="Install King iOS" FontSize="28" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                <TextBlock Text="{Binding DeviceName, StringFormat='Device: {0}'}" FontSize="14" Foreground="#A0A0B0" HorizontalAlignment="Center" Margin="0,5,0,30"/>

                <!-- Keep Data Toggle -->
                <Border Background="#16213E" CornerRadius="12" Padding="25" Margin="0,0,0,20">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Keep Data" FontSize="18" FontWeight="SemiBold" Foreground="White"/>
                            <TextBlock TextWrapping="Wrap" FontSize="13" Foreground="#A0A0B0" Margin="0,5,20,0">
                                When enabled, your current iPhone data will be backed up before installation.
                                A folder named "OldiPhoneData" will be created on your device to store your files.
                            </TextBlock>
                        </StackPanel>
                        <ToggleButton Grid.Column="1" 
                                      IsChecked="{Binding KeepData}" 
                                      Style="{StaticResource ToggleSwitch}"
                                      VerticalAlignment="Center"/>
                    </Grid>
                </Border>

                <!-- Features Section -->
                <TextBlock Text="Features" FontSize="22" FontWeight="Bold" Foreground="White" Margin="0,0,0,15"/>

                <ItemsControl ItemsSource="{Binding AvailableFeatures}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#16213E" CornerRadius="12" Padding="20" Margin="0,0,0,12">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="SemiBold" Foreground="White"/>
                                        <TextBlock Text="{Binding Description}" FontSize="12" Foreground="#A0A0B0" Margin="0,3,20,0" TextWrapping="Wrap"/>
                                    </StackPanel>
                                    <CheckBox Grid.Column="1" 
                                              IsChecked="{Binding IsEnabled}"
                                              VerticalAlignment="Center"
                                              Foreground="#E94560"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Proceed Button -->
                <Button Content="Start Installation" 
                        Command="{Binding ProceedCommand}"
                        Style="{StaticResource PrimaryButton}"
                        Height="55"
                        FontSize="18"
                        Margin="0,20,0,0"/>

                <TextBlock Text="{Binding StatusMessage}" 
                           FontSize="12" 
                           Foreground="#A0A0B0" 
                           HorizontalAlignment="Center" 
                           Margin="0,10,0,0"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>